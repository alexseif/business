<form method="post" action="{{ path('app_tracker_delete', {'id': tracker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tracker.id) }}">
    <button class="btn">Delete</button>
</form>
