<form method="post" action="{{ path('app_time_management_delete', {'id': time_management.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ time_management.id) }}">
    <button class="btn">{{ 'actions.delete'|trans }}</button>
</form>
