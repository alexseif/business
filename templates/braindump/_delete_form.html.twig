<form method="post" action="{{ path('app_braindump_delete', {'id': braindump.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ braindump.id) }}">
    <button class="btn">{{ 'actions.delete'|trans }}</button>
</form>
